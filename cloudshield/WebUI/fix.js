const fs = require('fs'); let code = fs.readFileSync('src/pages/__tests__/EmployeesPage.test.jsx', 'utf-8'); code = code.replaceAll('expect(patchCalls.length).toBeGreaterThanOrEqual(1);', 'const patchCalls = global.fetch.mock.calls.filter(call => call[1]?.method === \'PATCH\'); expect(patchCalls.length).toBeGreaterThanOrEqual(1);'); fs.writeFileSync('src/pages/__tests__/EmployeesPage.test.jsx', code);
